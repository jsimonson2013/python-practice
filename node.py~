class Node:
    def __init__ (self, data, nextNode):
        self.data = data
        self.nextNode = nextNode

    def hasNext(self):
        if (self.nextNode == None):
            return False
        else:
            return True
